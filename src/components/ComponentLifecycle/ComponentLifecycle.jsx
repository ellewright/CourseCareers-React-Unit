export default function ComponentLifecycle() {
    return (
        <div>Component Lifecycle</div>
    )
}

// Mounting : Component is first rendered to page
// Updating : Rerenders due to state change in self or parent
// Unmounting : Component is removed from page, states reset to initial value